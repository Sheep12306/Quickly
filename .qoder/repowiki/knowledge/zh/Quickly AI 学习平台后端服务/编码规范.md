- 所有受保护的业务接口均通过 `Depends(get_current_user)` 注入当前登录用户，实现统一的身份鉴权。
- 数据库会话通过 `Depends(get_db)` 依赖注入，确保每个请求拥有独立的 AsyncSession 并在结束后自动关闭。
- API 响应模型严格遵循 Pydantic Schema 定义，并在路由装饰器中通过 `response_model` 声明输出结构。
- 业务逻辑中涉及多表写入时（如注册时同时创建用户与设置），使用 `db.flush()` 获取主键后再执行关联插入。